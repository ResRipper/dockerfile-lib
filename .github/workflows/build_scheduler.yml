# Copyright 2026 ResRipper.
# SPDX-License-Identifier: Apache-2.0

name: Build-scheduler

on:
    workflow_dispatch:
    # schedule:
    #     - cron: "0 0 1 * *"

jobs:
    # caddy-custom:
    #     uses: ./.github/workflows/caddy-custom.yml
    #     with:
    #         push: true

    # grav:
    #     uses: ./.github/workflows/grav.yml
    #     with:
    #         push: true

    shadowsocks-rust:
        runs-on: ubuntu-latest
        steps:
            -   name: Build and push
                id: build
                uses: ./.github/workflows/shadowsocks-rust.yml
                with:
                    push: true
            -   name: Scan
                uses: ./.github/workflows/scanner.yml
                with:
                    image: ${{ steps.build.outputs.tag }}